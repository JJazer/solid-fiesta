<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.best.dex.endllessgame">

    <application
        android:theme="@android:style/Theme.Holo.NoActionBar.Fullscreen" >
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EndlessRunningGameActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".GameView" />
        <activity android:name=".GameLoopThread"></activity>
        <service android:name=".GodOfSound"  android:enabled="true"></service>
    </application>

</manifest>
